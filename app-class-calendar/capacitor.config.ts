import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-class-calendar',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
